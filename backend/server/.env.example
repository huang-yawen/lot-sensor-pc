PORT=3000
HOST=127.0.0.1
# 逗号分隔；只有明确可信的前端来源才应加入。
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173
FRONTEND_DIST_PATH=

# 离线指令持久化位置；留空时使用 server/data/pending-commands.json
PENDING_COMMANDS_FILE=

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=task
DB_CONNECTION_LIMIT=10

MQTT_URL=mqtt://10.97.241.240:1883
MQTT_CLIENT_ID=lot-sensor-pc
MQTT_USERNAME=
MQTT_PASSWORD=
