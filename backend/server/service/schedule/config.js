/**
 * 【文件职责】定时开关的总开关与检查周期。定时开关：到了用户在指令页设定的时刻，
 * 自动把绑定的执行器开关置为指定值（如 8:00:00 开水泵）。逻辑在同目录 scheduleService.js。
 * 【值的来源】新增功能，这里是初始生效值。改完重启后端生效。
 * 【谁在读】scheduleService.js 本身。
 *
 * 说明：几点钟做什么事不写在这里，全部来自指令中心 t_direct_config 里的时间框
 * 指令项（f_type='4'），换赛题只改数据库、不动代码。这里只放"要不要跑"和纯软件参数。
 */
module.exports = {
  enabled: true,        // 定时开关总开关。关掉后所有定时项都不执行，页面上仍可正常设置时间

  // 自检周期（毫秒）。触发精度是秒级，所以这个值必须小于 1000，否则会整秒跳过、
  // 导致设定的时刻被漏掉。默认 500 表示每一秒至少检查两次，同一秒内的重复检查由
  // scheduleService 的秒级去重挡住，不会重复下发。
  // 后端启动时读一次，改了要重启后端才生效。
  tickIntervalMs: 500,
}
